/**
 * Призначення: Feedback360 / presentation — modules/feedback360/presentation/http/controllers/competence-clusters.controller.ts.
 * З чим пов’язаний: presentation layer (HTTP) цього bounded context.
 * Що має бути: ендпоінти для ресурсу CompetenceCluster (DTO-валидація, виклик application service, відповідь).
 */
