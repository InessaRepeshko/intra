/**
 * Призначення: Feedback360 / presentation — modules/feedback360/presentation/http/controllers/question-position-relations.controller.ts.
 * З чим пов’язаний: presentation layer (HTTP) цього bounded context.
 * Що має бути: ендпоінти для ресурсу QuestionPositionRelation (DTO-валидація, виклик application service, відповідь).
 */
