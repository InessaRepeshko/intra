/**
 * Призначення: Feedback360 / presentation — modules/feedback360/presentation/http/controllers/questions.controller.ts.
 * З чим пов’язаний: presentation layer (HTTP) цього bounded context.
 * Що має бути: ендпоінти для ресурсу Question (DTO-валидація, виклик application service, відповідь).
 */
