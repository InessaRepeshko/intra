/**
 * Призначення: Feedback360 / application — modules/feedback360/application/repository-ports/cluster-user-relation.repository.port.ts.
 * З чим пов’язаний: порт (інтерфейс) репозиторію для application layer.
 * Що має бути: контракт доступу до даних для сутності ClusterUserRelation (без Prisma/NestJS).
 */
