/**
 * Призначення: Feedback360 / application — modules/feedback360/application/repository-ports/question-position-relation.repository.port.ts.
 * З чим пов’язаний: порт (інтерфейс) репозиторію для application layer.
 * Що має бути: контракт доступу до даних для сутності QuestionPositionRelation (без Prisma/NestJS).
 */
