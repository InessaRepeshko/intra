/**
 * Призначення: Feedback360 / domain — modules/feedback360/domain/question/question.mapper.ts.
 * З чим пов’язаний: мапінг між шарами (domain ↔ persistence/HTTP).
 * Що має бути: перетворення для Question (Prisma record ↔ domain, domain ↔ entity).
 */
