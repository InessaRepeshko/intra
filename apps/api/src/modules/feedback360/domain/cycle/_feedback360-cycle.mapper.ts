/**
 * Призначення: Feedback360 / domain — modules/feedback360/domain/cycle/feedback360-cycle.mapper.ts.
 * З чим пов’язаний: мапінг між шарами (domain ↔ persistence/HTTP).
 * Що має бути: перетворення для Feedback360Cycle (Prisma record ↔ domain, domain ↔ entity).
 */
