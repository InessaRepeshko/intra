/**
 * Призначення: Identity / domain — modules/identity/domain/position/position.mapper.ts.
 * З чим пов’язаний: мапінг між шарами (domain ↔ persistence/HTTP).
 * Що має бути: перетворення для Position (Prisma record ↔ domain, domain ↔ entity).
 */
