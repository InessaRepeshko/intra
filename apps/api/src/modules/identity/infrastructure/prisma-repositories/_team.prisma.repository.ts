/**
 * Призначення: Identity / infrastructure — modules/identity/infrastructure/prisma-repositories/team.prisma.repository.ts.
 * З чим пов’язаний: infrastructure adapter на Prisma (реалізація repository port).
 * Що має бути: Prisma-запити до моделі Team та мапінг у домен/HTTP-моделі.
 */
