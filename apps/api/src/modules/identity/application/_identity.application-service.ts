/**
 * Призначення: Identity / application — modules/identity/application/identity.application-service.ts.
 * З чим пов’язаний: application layer (use-cases) цього bounded context.
 * Що має бути: сценарії (оркестрація), виклики портів/репозиторіїв, перевірка інваріантів домену.
 */
