/**
 * Призначення: Identity / application — modules/identity/application/repository-ports/team.repository.port.ts.
 * З чим пов’язаний: порт (інтерфейс) репозиторію для application layer.
 * Що має бути: контракт доступу до даних для сутності Team (без Prisma/NestJS).
 */
