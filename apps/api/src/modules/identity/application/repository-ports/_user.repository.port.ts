/**
 * Призначення: Identity / application — modules/identity/application/repository-ports/user.repository.port.ts.
 * З чим пов’язаний: порт (інтерфейс) репозиторію для application layer.
 * Що має бути: контракт доступу до даних для сутності User (без Prisma/NestJS).
 */
