/**
 * Призначення: Identity / application — modules/identity/application/repository-ports/position.repository.port.ts.
 * З чим пов’язаний: порт (інтерфейс) репозиторію для application layer.
 * Що має бути: контракт доступу до даних для сутності Position (без Prisma/NestJS).
 */
