/**
 * Призначення: Identity / application — modules/identity/application/security-ports/password-hasher.port.ts.
 * З чим пов’язаний: порт для взаємодії між контекстами (anti-corruption boundary).
 * Що має бути: мінімальний read-only контракт, який потрібен цьому контексту від сусіднього.
 */
