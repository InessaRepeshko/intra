/**
 * Призначення: Identity / presentation — modules/identity/presentation/http/dto/update-position.dto.ts.
 * З чим пов’язаний: DTO для HTTP-запитів/відповідей (валидація, Swagger).
 * Що має бути: дані для операції над ресурсом UpdatePosition (поля + декоратори).
 */
