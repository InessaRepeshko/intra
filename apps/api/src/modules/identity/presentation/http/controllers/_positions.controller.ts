/**
 * Призначення: Identity / presentation — modules/identity/presentation/http/controllers/positions.controller.ts.
 * З чим пов’язаний: presentation layer (HTTP) цього bounded context.
 * Що має бути: ендпоінти для ресурсу Position (DTO-валидація, виклик application service, відповідь).
 */
