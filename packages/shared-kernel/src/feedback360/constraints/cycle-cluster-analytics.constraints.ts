import { GLOBAL_CONSTRAINTS } from "../../common/constraints/global.constraints";

export const CYCLE_CLUSTER_ANALYTICS_CONSTRAINTS = {
    EMPLOYEES_COUNT: {
        MIN: 1,
    },
    SCORE: {
        MIN: 0,
        MAX: 5,
    },
};